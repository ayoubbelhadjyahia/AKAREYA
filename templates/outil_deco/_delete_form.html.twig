<form method="post" action="{{ path('app_outil_deco_delete', {'id': outil_deco.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ outil_deco.id) }}">
    <button class="btn btn-danger"><i class="bi bi-trash3-fill"></i></button>
</form>
