{% extends 'baseBack.html.twig' %}

{% block title %}OutilDeco{% endblock %}
{% block hero %}

    <div class="breadcrumbs d-flex align-items-center" style="background-image: url({{ asset('assets/img/services-header.jpg') }})">
        <div class="container position-relative d-flex flex-column align-items-center">

            <h2>Gestion de decoration</h2>
            <ol>
                <li><a href="http://127.0.0.1:8000/dossier/const/home">Home</a></li>
                <li>gestion de decoration</li>
            </ol>

        </div>
    </div>

{% endblock %}

{% block body %}
<div class="container position-relative aos-init aos-animate" data-aos="fade-up">
    <h1 align="center"> OutilDeco</h1>

    <table class="table table-dark">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ outil_deco.id }}</td>
        </tr>
        <tr>
            <th>Type_outil_d</th>
            <td>{{ outil_deco.typeOutilD }}</td>
        </tr>
        <tr>
            <th>Date_mise_Service</th>
            <td>{{ outil_deco.DateMiseService ? outil_deco.DateMiseService|date('Y-m-d') : '' }}</td>
        </tr>
        <tr>
            <th>Prix_outil</th>
            <td>{{ outil_deco.prixOutil }}</td>
        </tr>
        </tbody>
    </table>
    <button class="btn">
        <a class="bi bi-chevron-bar-left" href="{{ path('app_outil_deco_indexAdmin') }}"><ion-icon name="chevron-back-outline"></ion-icon></a>
    </button>
    <hr>
    <button class="btn">
        <a  class="bi-tools" href="{{ path('app_outil_deco_editAdmin', {'id': outil_deco.id}) }}"><i class="bi-tools"></i><ion-icon name="build-outline"></ion-icon></a>
    </button>
    <hr>

    {{ include('outil_deco/_deleteAdmin_form.html.twig') }}
</div>
{% endblock %}
