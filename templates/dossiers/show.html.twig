{% extends 'base.html.twig' %}

{% block title %}Dossiers{% endblock %}

{% block body %}
    <h1>Dossiers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dossier.id }}</td>
            </tr>
            <tr>
                <th>Date_commande</th>
                <td>{{ dossier.dateCommande }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ dossier.nombre }}</td>
            </tr>
            <tr>
                <th>Id_client</th>
                <td>{{ dossier.idClient }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dossiers_index') }}">back to list</a>

    <a href="{{ path('app_dossiers_edit', {'id': dossier.id}) }}">edit</a>

    {{ include('dossiers/_delete_form.html.twig') }}
    <button id ="a" class="btn" style="background:deepskyblue">
        <a  style="color: #FFFFFF" >mail</a>
    </button>




    </div>


    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>

    <script>
        $('#a').on('click', function() {


            $.ajax({
                method: "POST",
                url: "http://127.0.0.1:8000/dossierss/mail",
                data: { email: "fourat.cherif@esprit.tn",body:"Mr/Mm consulter notre site pour voir les modification affectuer"
                }
            })
                .done(function( msg ) {
                    if(msg == "true") {
                        alert(" votre mail est bien recus! ");
                        window.location.replace("http://127.0.0.1:8000/dossierss/");

                    }
                } )
        });
    </script>
{% endblock %}
